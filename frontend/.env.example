# Frontend env vars (Vite-style — only VITE_* prefix is exposed to the browser)
VITE_FLASK_URL=http://localhost:5000

# Optional second backend (Express proxy) for OCR / Gemini-backed text features.
# Leave blank to use the in-app mocks for those flows.
VITE_BACKEND_URL=
